{
  "name": "open-payments-specifications",
  "description": "OpenAPI specifications for the Open Payments APIs",
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "lint:openapi": "spectral lint openapi/*",
    "generate:json": "redocly bundle openapi/auth-server.yaml --output json/auth-server.json --ext json && redocly bundle openapi/resource-server.yaml --output json/resource-server.json --ext json && redocly bundle openapi/wallet-address-server.yaml --output json/wallet-address-server.json --ext json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@redocly/cli": "^2.31.2",
    "@stoplight/spectral-cli": "^6.15.0",
    "prettier": "3.6.2"
  },
  "packageManager": "pnpm@10.14.0"
}
